import java.util.*;
public class alpha
{
public static void main(String args[])
{
int i;
Scanner sc = new Scanner(System.in);
System.out.println("Enter the Elements : ");
char ch[] = new char[20];
for(i=0;i<arr.length();i++)
	arr[i]=scan.charAt(i);
for(i=0;i<arr.length();i++)
	if(arr[i]>=97 && arr[i]<=122 || arr[i]>=65 && arr[i]<=122 || arr[i] >= 49 && arr[i] <=57 || arr[i] >=32 && arr[i] <= 47)
	{
		System.out.println(+(arr[i]));
		
	}
}
}